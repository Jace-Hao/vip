# 洗衣管家 · 会员数据导出工具 — 开发交接文档

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v1.6（2026-09-24） |
| 工具目录 | `E:\软件开发\洗衣管家会员导出` |
| 代码仓库 | <https://github.com/Jace-Hao/vip>（main 分支） |
| 服务对象 | 洗衣管家 v6.1.x（门店收银/会员软件） |
| 维护约定 | 本文档是最完整的交接材料；README.md 是 v1.6 功能速览；使用说明.html 停留在 v1.5 时代 |

---

## 1. 项目概览

一套**只读**的门店数据工具：从「洗衣管家」软件里导出会员数据、逐会员详情和全部订单明细，生成离线可搜索的「会员数据查询台」，并配套一个带定时同步、自动关机、开机自启、自动清理的抓取操作台。

| 能力 | 入口 | 说明 |
| --- | --- | --- |
| 会员导出（常规+全量字段） | `导出会员数据.cmd` | 几秒完成，常规 9 列 + 档案全字段 62 列 |
| 逐会员详情导出（增量） | `导出会员数据(含详情).cmd` | 标签/余额构成/卡券/订单号/最近充值；签名比对增量，只抓新增/变更 |
| 订单明细抓取 | 操作台 或 `fetch_orders.mjs` | 逐单完整详情（衣物/照片链接/支付/状态历史），断点续传 |
| 会员数据查询台 | `打开会员查询页面.cmd` | 离线单页，搜索/筛选/订单详情/卡券/订单号反查 |
| 订单抓取操作台 | `打开订单抓取操作台.cmd` | 开始/暂停/进度/日志/定时同步/同步后关机/手动清理 |

**原则：全程只读**。所有取数通过软件的查询接口完成，不写入、不点击界面。

---

## 2. 运行环境与依赖

| 依赖 | 位置 / 版本 | 说明 |
| --- | --- | --- |
| 操作系统 | Windows 10/11 中文环境 | 门店工作机即开发机 |
| Node.js | `D:\node.js\node.exe`（v22+，实测 v24 也可） | **零第三方依赖**，脚本全部是 ESM `.mjs` |
| Python | `%LOCALAPPDATA%\Python\bin\python.exe` | 仅 `make_xlsx.py`（openpyxl）与 `build_viewer.py`（标准库）使用 |
| 洗衣管家 | `D:\Blending_Release-6.1.17\xygjwinapp.exe` | 必须带 `--remote-debugging-port=9222` 启动才能取数 |
| 计划任务 | `XQY-Orders-Console` | 开机自启（登录后 30 秒，wscript 隐藏启动） |

编码注意：`.cmd` 必须 **GBK + CRLF**；PowerShell 脚本中文注意保存编码；跑 Python 输出带 `¥` 等字符时加 `-X utf8` 与 `PYTHONIOENCODING=utf-8`。

---

## 3. 取数原理（核心架构）

所有数据通过 **CDP（Chrome DevTools Protocol，端口 9222）注入页面上下文**，调用洗衣管家软件自身的查询接口：

```
找 9222 调试端口的目标页面
  → Runtime.evaluate 在页面内执行包装函数
  → 页面内用 $request 发软件自己的 XHR（带软件的登录态）
  → 拿到 JSON 返回
```

| 接口 | 用途 | 备注 |
| --- | --- | --- |
| `getuinfo` | 会员详情（含卡券、订单数） | 详情导出主接口 |
| `searchwashorders` | 订单列表（按 uid 分页） | `start/size/currentPage/currenQuantity`（注意拼写 curren） |
| `getorderdetail` | 单笔订单完整详情 | 约 15.6KB/单：114 字段 + 衣物数组 + 三套照片 URL + 支付 plist + 状态历史 |
| `getordercloth` | 订单衣物 | 实测无需（getorderdetail 已含） |

**关键坑：页面内 `$request` 的回调在非成功时可能永远不触发** → 必须自带超时兜底（页内 25s + evaluate 外层 40s），否则整个流程挂死。

**服务端限速**：请求达到数百次后服务端会冷却（实测 30~40 分钟不响应），营业时段更严重（白天几乎全部超时）。应对策略（已内建）：失败自动等待 180 秒重试、断点续传、夜间定时同步避开营业时段。**慢不丢数据，只要一直跑就能抓完。**

---

## 4. 目录结构

```
洗衣管家会员导出\
├─ 导出会员数据.cmd            ← 日常导出入口（常规+全量字段）
├─ 导出会员数据(含详情).cmd     ← 完整导出（+详情/卡券/订单号，增量）
├─ 打开会员查询页面.cmd         ← 查询台入口（先刷新数据再开浏览器）
├─ 打开订单抓取操作台.cmd       ← 操作台入口（服务已在后台时只开页面）
├─ 以调试模式启动洗衣管家.cmd   ← 应急：以调试端口重启洗衣管家
├─ 安装开机自启.cmd            ← 注册开机自启计划任务
├─ 移除开机自启.cmd            ← 移除上述计划任务
├─ 开机自启-操作台服务.vbs      ← 计划任务实际执行的无窗口启动器
├─ 操作页面.html               ← 操作台前端（单文件）
├─ 导出流程图.png / 操作台运行流程图.png
├─ README.md / 使用说明.html / 开发交接文档.md / .html
├─ app\
│  ├─ export.mjs               ← 会员导出+详情增量（memberSig 签名比对、--deep/--deep-full）
│  ├─ fetch_orders.mjs         ← 订单抓取（--uid/--sample/--all、断点续传、单实例锁）
│  ├─ orders_server.mjs        ← 操作台本地服务（127.0.0.1:8791，含定时/关机/清理）
│  ├─ build_viewer.py          ← 生成查询页 data.js + 订单分片 + 索引
│  └─ make_xlsx.py             ← Excel 生成（openpyxl）
├─ 查询页面\
│  ├─ index.html               ← 查询台（离线单页）
│  ├─ data.js                  ← 全部会员+详情+订单索引（自动生成）
│  └─ 订单分片\part_NNN.js      ← 订单数据分片（60 人/片，页面按需加载）
└─ 导出结果\                    ← 所有导出/抓取产物（gitignore，绝不入库）
```

---

## 5. 数据文件说明（接手必读）

### 5.1 带时间戳的导出产物

每次「含详情导出」或「定时同步」都会在 `导出结果\` 生成一套同时间戳文件（`_YYYYMMDD_HHMMSS`）：

- `会员导出_*.json / .csv` — 常规 9 列 + 全量 62 列（json 含 `fullRows`，是查询台的会员主源）
- `会员全量数据_*.csv / .json / .xlsx` — 全量字段表
- `会员详情_*.json / .xlsx` — 逐会员详情快照（json 内 `details` 数组，1962 条/全量）

`导出结果\订单数据\` 内：`订单_*.json`（单会员抓取产物）、`会员订单_*.json`（批量合并快照）、`订单试点预览_*.html`（试点可视化）。

### 5.2 点开头的文件 = 正在使用的运行数据（清理功能永不触碰，手动清理也请保留）

| 文件 | 作用 | 丢了会怎样 |
| --- | --- | --- |
| `导出结果\订单数据\.orders_results.jsonl` | **订单抓取基线**：每行一个会员的完整订单 JSON（含 sig 签名、complete 标记）。增量比对、断点续传、查询台订单数据全部以它为主源 | 下次同步把全部 1911 位有订单会员的订单**重抓一遍**（数小时） |
| `导出结果\.detail_cache.json` | 会员详情签名缓存（1962 条） | 详情增量失效 → 详情全部重抓 |
| `导出结果\.deep_results.jsonl` | 详情抓取续传记录 | 影响同上（较轻） |
| `导出结果\.last_export.json` | 最近一次导出的元信息 | 无关紧要 |
| `导出结果\订单数据\.orders_run.lock` | 抓取单实例锁（内容=PID，带存活检测） | 陈旧锁会被自动清理，无需手动处理 |
| `.sync_schedule.json`（工具根目录） | 定时同步设置（enabled/time/shutdownAfter/lastFiredDate） | 定时设置回到默认（停用） |

> 真实教训（2026-09-24）：手动清空「订单数据」文件夹时把 `.orders_results.jsonl` 一并删了，导致续传基线丢失（已从回收站恢复）。自动清理对点开头文件永久豁免；人工清理时请同样避开。

### 5.3 查询台数据

- `查询页面\data.js`（约 5.7MB）：会员列表 + 全部详情 + `orderIndex/orderSearch`（订单号反查会员）。
- `查询页面\订单分片\part_NNN.js`：订单详情按 60 人/片懒加载，页面用到哪片注入哪片。
- 由 `build_viewer.py` 生成；它**只读最新一份** `会员导出_*.json` 与 `会员详情_*.json`，订单以 `.orders_results.jsonl` 为主源、`订单_*.json` 为补充。

### 5.4 自动清理（v1.6 新增）

- 规则：按文件名时间戳分批，**保留最近 2 批**，其余移入**回收站**（可找回）。
- 触发：服务启动后 8 秒、每次抓取完成后（顺序：清理 → 刷新查询页 → 可选关机）、操作台按钮「清理过时数据」。
- 被占用的文件（如正开着的 Excel）自动跳过。

---

## 6. 操作台与自动化机制

### 6.1 操作台服务（orders_server.mjs）

- 地址 `http://127.0.0.1:8791`（仅本机）；由计划任务开机自启，或双击 `打开订单抓取操作台.cmd`（已有实例时只开页面）。
- 状态机：`idle → deep（详情增量）→ orders（订单抓取）→ done/failed`，`paused` 为暂停。
- API：`GET /api/status`、`POST /api/start`（`{"sample":N}` 可选）、`/api/stop`、`/api/rebuild`、`/api/cleanup`、`/api/schedule`（GET/POST）、`/api/cancel_shutdown`、`/api/open`。
- 抓取流水线：`export.mjs --deep`（详情增量）→ `fetch_orders.mjs --all`（订单，sig 比对跳过未变会员，`complete:false` 的自动补抓）。
- 失败处理：成员级失败等 180 秒重试一次；成员内失败订单等 180 秒统一重试一轮；仍失败标记 `complete:false`，后续运行自动补抓；连续失败过多自动停止（保留进度）。
- 单实例锁 `.orders_run.lock`（PID 存活检测）；重复自启的实例会因端口占用静默退出，且**不会误删**抓取中的锁。

### 6.2 定时同步（页面设置，存 `.sync_schedule.json`）

- 触发条件（全部满足）：已启用、当前时间与设定时刻相差 ≤10 分钟、今天还没触发过、无正在运行的任务、锁文件不存在（或已死）。
- 到点流程：确保洗衣管家在调试模式（端口不通 → 自动 taskkill 后带 `--remote-debugging-port=9222` 重启，最多等 90 秒）→ 详情增量 → 订单抓取 → 自动清理 → 刷新查询页 →（勾选时）约 2 分钟后关机。
- **任一环节失败都不会关机**（软件重启失败、同步取消等情况直接终止本次定时任务）。
- 关机倒计时 120 秒，页面顶部红色提示条可一键取消（`shutdown /a`）。
- 错过设定时刻超过 10 分钟视为错过，次日再触发；每日循环，直到用户停用。

### 6.3 开机自启

- 计划任务 `XQY-Orders-Console`：登录 Windows 后 30 秒，`wscript` 隐藏运行 `开机自启-操作台服务.vbs`（无窗口、不弹浏览器）。
- 重装：双击 `安装开机自启.cmd`；移除：`移除开机自启.cmd`。
- 关机后次日开机登录，服务自动就位 → 当晚定时同步照常。

---

## 7. 安全红线

1. **会员数据绝不进仓库、绝不外传**。`.gitignore` 覆盖：`导出结果/`、`查询页面/data.js`、`查询页面/订单分片/`、`.dev/`、`.sync_schedule.json` 等。每次 commit 前核对 `git status`，出现数据文件立即中止。
2. 对洗衣管家**只读**：只调查询接口，不写数据、不点界面。
3. 营业时间**不重启洗衣管家**（店员正在使用；需要调试端口时等打烊或与店员协调）。
4. 查询台/导出文件仅限门店内部使用。

---

## 8. 版本历史

| 版本 | 日期 | 内容 |
| --- | --- | --- |
| v1.0 | 09-22 | 常规 9 列导出 |
| v1.1 | 09-22 | 全量字段（62 列）+ 会员详情（标签/卡券/订单号，断点续传） |
| v1.2 | 09-22 | 会员数据查询台（离线单页），导出后自动刷新 |
| v1.3 | 09-22 | 抓取失败自动等 180 秒重试；30 分钟无进展自动停止 |
| v1.4 | 09-22 | 深度详情**增量更新**（memberSig 签名比对，--deep-full 强制全量） |
| v1.5 | 09-24 | 查询台整合**订单详情**（分片懒加载/订单号反查/待补抓合并显示：会员快照比抓取快照新的订单显示摘要卡并自动补齐） |
| v1.6 | 09-24 | **订单抓取操作台**（8791 服务+页面：开始/暂停/进度/日志）；**定时同步**（每日自动抓取+同步后自动关机可取消）；订单失败 180 秒重试与补抓标记；**开机自启**（XQY-Orders-Console）；**自动清理**（保留最近 2 批，回收站）；jsonl 过期 20 小时→7 天 |

---

## 9. 已知限制与踩坑记录

### 关机链路（重要）
- 「同步完成后自动关机」与定时功能**解耦**：只看操作台「同步完成后自动关机」勾选项；不开启定时、手动点「开始抓取」跑完同样会关机。
- 根因坑：`shutdown.exe` 对环境块敏感——操作台服务若从精简环境（缺 `COMPUTERNAME`/`ComSpec` 等）拉起，子进程继承残缺环境，shutdown 报 203（ERROR_ENVVAR_NOT_FOUND）**静默失败**，日志无异常但关机不执行。
- 修复：orders_server.mjs 内置 SHUTDOWN_ENV 兜底补全关键变量（COMPUTERNAME 缺失时用 os.hostname()），全路径调用 System32\shutdown.exe；doShutdown 检查 exit code，失败写日志并取消倒计时。
- 验证方法：node 用同款 env 构造 spawn `shutdown /s /t 120`，返回 0 后立刻 `shutdown /a` 取消（勿真关机）。

| # | 事项 | 说明 / 对策 |
| --- | --- | --- |
| 1 | 服务端限速 | 约 400 次请求后冷却 30~40 分钟；营业时段更严重。内建 180 秒等待+重试+续传，夜间跑最稳 |
| 2 | `$request` 回调不触发 | 非 success 时回调可能永不执行 → 所有调用必须带超时兜底 |
| 3 | build_viewer 只取最新 | `会员导出_*.json`/`会员详情_*.json` 各只读最新一份；自动清理保留 2 批与其兼容 |
| 4 | 订单快照时间差 | 会员数据（列表）比订单抓取晚时，会出现「5 笔 vs 4 笔」——查询台已做合并显示（摘要卡+待补抓标注） |
| 5 | 服务内状态 vs 模块级变量 | `start()` 会重置 state 对象；`scheduleTriggered` 这类跨阶段标记必须放模块级，且 deep 失败/暂停路径都要复位（否则可能在错误时机关机） |
| 6 | 重复实例与锁 | 端口占用时自启实例静默退出；`process.on('exit')` 清锁前必须检查锁内 PID 是否存活，防止误删抓取中任务的锁 |
| 7 | 仓库文件名大小写 | 仓库里是小写 `readme.md`（Windows 大小写不敏感，`git add README.md` 不报错但也不生效） |
| 8 | 临时目录会被清理 | `.openclaw\tmp` 类临时目录有外部自动清理机制，重要产物必须落进项目目录或入库 |
| 9 | jsonl 过期 | `fetch_orders.mjs` 中 jsonl 超过 7 天未更新才会重置（v1.6 前是 20 小时，跨天续传会全量重抓） |
| 10 | git 提交显示乱码 | 控制台 GBK 显示问题，实际提交内容是正确的 UTF-8，勿因此反复重试 |

---

## 10. 当前进度与待办（2026-09-24 13:55）

| 事项 | 状态 |
| --- | --- |
| 订单详情抓取 | **487 / 1911 人**（1200 单）已完成并保存；抓取处于暂停状态，待 19:00 定时同步自动续传 |
| 定时同步设置 | 用户已设「每天 19:00 · 同步后自动关机」，当天 19:00 首次生效 |
| 今晚 21:00 自动任务（cron 417c5eca） | 详情增量 + 订单抓取 + 微信简报（与页面定时同步有锁避让，不冲突；电脑若已关机则自然跳过） |
| 明早 07:30 检查（cron 75dd1ccf） | 查进度 + 刷新查询页（电脑关机中则开机登录后操作台服务自动就位） |
| 杨美霞（uid 25190368）第 5 单 | 183173138（¥531.70，09-23 新增）待补抓，查询台当前显示摘要卡「详情待补抓」，补抓后自动升级为完整卡 |
| 战福丽 | onum>0 但接口返回 0 单，待全量抓取完成后复核 |

**给接手者的第一晚预期**：19:00 定时同步自动重启软件进调试模式 → 续传订单（数小时，限速等待属正常）→ 完成后自动刷新查询页 → 自动关机。次日查询台即含全部订单。

---

## 11. 接手清单（常见操作速查）

| 想做什么 | 操作 |
| --- | --- |
| 日常导出会员 | 双击 `导出会员数据.cmd` |
| 完整导出（含详情） | 双击 `导出会员数据(含详情).cmd` |
| 查看会员/订单数据 | 双击 `打开会员查询页面.cmd` |
| 手动抓订单/看进度 | 双击 `打开订单抓取操作台.cmd` → 开始/暂停/日志 |
| 改定时同步时间/关机 | 操作台页面「定时同步」卡片 → 修改后保存 |
| 手动清理旧数据 | 操作台「清理过时数据」按钮（或等自动触发） |
| 重装/移除开机自启 | 双击 `安装开机自启.cmd` / `移除开机自启.cmd` |
| 软件调试端口没开 | 关闭洗衣管家 → 双击 `以调试模式启动洗衣管家.cmd`（操作台定时触发时也会自动处理） |
| 改了代码如何生效 | 操作台相关：重启服务（`schtasks /Run /TN "XQY-Orders-Console"` 或杀掉 8791 进程后双击 cmd）；抓取脚本相关：下一次任务启动自动用新代码 |
| 提交代码 | 只提交代码/文档；数据文件已在 .gitignore；提交后 `git push`（仓库 <https://github.com/Jace-Hao/vip>） |

> 文档生成：本 md 为源文件，html 由 `md2doc.js` 渲染（改内容请改 md 后重跑）。
